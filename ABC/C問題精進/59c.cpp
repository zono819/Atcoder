#include<bits/stdc++.h>
using namespace std;
long long F(vector<int> &a){
  int n=a.size();
}
signed main(){
  cin.tie(nullptr);
  ios::sync_with_stdio(false);
  int n,c=0,d;
  long long ans=0;cin>>n;
  vector<int> a(n),b(n+1,0);
  for(int i=0;i<n;i++){
    cin>>a[i];
    b[i+1]=b[i]+a[i];
    if()
  }
  cout<<ans;
}